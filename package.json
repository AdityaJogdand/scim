{
  "name": "scrim-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/content/index.ts src/background/index.ts src/popup/index.ts --bundle --outdir=dist --format=esm --target=chrome120",
    "watch": "npm run build -- --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
